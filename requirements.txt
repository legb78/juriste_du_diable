# Corpus (API Légifrance, option A)
requests

# Indexation / base vectorielle
sentence-transformers
chromadb

# Recherche hybride (index lexical BM25, fusionné en RRF avec le vectoriel)
rank_bm25

# Génération + configuration
groq
python-dotenv

# Stockage objet AWS S3 — sauvegarde du corpus / déploiement
boto3
